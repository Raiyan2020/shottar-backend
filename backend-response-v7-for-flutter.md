# رد الباك إند على backend-requirements v7

> تاريخ التنفيذ: **19 أغسطس 2026** · Base URL: `https://shottarapp.com/api`
>
> الملف ده بيمشي على نفس ترقيم بنود v7 بالترتيب: إيه اللي اتعمل، وإيه اللي محتاج
> شغل من التطبيق، وإيه اللي محتاج إدخال بيانات من لوحة التحكم.

## الخلاصة السريعة

| # | البند | الحالة |
|---|---|---|
| 1 | `grade_id` / `semester_id` في `/update-profile` | ✅ بيتحفظوا وبيرجعوا |
| 2 | تغيير الرقم بتحقّق OTP | ✅ endpointين جداد + `/update-profile` بقى بيتجاهل الرقم |
| 3 | `grade` / `semester` بنفس النوع | ✅ `int` أو `null` في كل الردود |
| 4 | `search` على `/subjects` | ✅ على مستوى الداتابيز (اسم المادة + المدرّس + المادة الأساسية) |
| 5 | `rating` / `tag` / `category` | ✅ رجعوا في الـ API — محتاجين إدخال بيانات من اللوحة |
| 6 | ملفات PDF | ✅ مسار جديد `download_url` بهيدرز وأكواد حالة صحيحة |
| 7 | JSON ولا form-data | ✅ الاتنين مقبولين — مؤكَّد بالاختبار |
| 8 | 401 على التوكن الملغي | ✅ **دي كانت بترجع 400** — اتصلحت |
| 9 | `terms` / `privacy_policy` | ✅ السبب اتصلح + أمر إصلاح للنص القديم |
| 10 | لينكات السوشيال | ✅ اللينك الغلط بيرجع `null` + حقل `whatsapp` جديد |
| 11 | حساب الخصم في `POST /order` | ✅ **الباك بقى هو اللي بيحسب** ويرجّع التفصيل |
| 12 | `progress_percent` | ✅ راجع في `/subjects` و `/subjects/purchased` |

---

## 1) `POST /update-profile` — `grade_id` / `semester_id`

بيتقبلوا وبيتحفظوا فعلًا (اختياريين — لو مش مبعوتين مش بيتغيروا). الرد بيرجّع
كائن الـ user بعد الحفظ من الداتابيز.

```
POST /update-profile
Authorization: Bearer <token>

name         = أحمد
grade_id     = 12
semester_id  = 2
image        = <file>        # اختياري
```

الرد (نفس شكل `data` القديم — كائن user مباشرة):

```json
{
  "status": true,
  "data": { "id": 42, "name": "أحمد", "grade": 12, "semester": 2, "...": "..." },
  "message": "تم تحديث الملف الشخصي بنجاح."
}
```

> ⚠️ الـ Postman collection موجودة في ريبو الفلاتر مش عندنا — محتاجة تتحدّث من
> عندكم بالحقلين، ويتشال منها `phone` / `country_code` من `/update-profile`.

---

## 2) تغيير رقم الجوال — endpointين جداد

`/update-profile` **بقى بيتجاهل `phone` و `country_code` تمامًا**. لو بعتّوهم
ومختلفين عن الرقم الحالي، الرد بيرجع ناجح بس الـ `message` بينبّه إن الرقم
متغيّرش. شكل `data` زي ما هو.

### أ) طلب الكود

```
POST /profile/phone/request-change
Authorization: Bearer <token>

phone        = 51234567
country_code = +965
```

- بيرفض لو الرقم مستخدم في حساب تاني → `422`
- بيرفض لو هو نفس رقمك الحالي → `422`
- بيبعت OTP على **الرقم الجديد** ومبيغيّر أي حاجة

```json
{
  "status": true,
  "data": {
    "phone": "51234567",
    "country_code": "+965",
    "expires_at": "2026-08-19T14:11:18+00:00",
    "expires_in": 600,
    "resend_after": 60
  },
  "message": "تم إرسال رمز التحقق إلى رقم الجوال الجديد."
}
```

### ب) تأكيد الكود

```
POST /profile/phone/confirm-change
Authorization: Bearer <token>

phone            = 51234567
country_code     = +965
activation_code  = 1234
```

- الكود الغلط → `422` مع `data.attempts_left`
- بعد 5 محاولات غلط → `429` ولازم طلب كود جديد
- الكود منتهي (10 دقايق) → `422`
- **الكود الصح بس** هو اللي بينقل الحساب

```json
{
  "status": true,
  "data": {
    "user": { "phone": "+96551234567", "country_code": "+965", "...": "..." },
    "token": "<توكن جديد>"
  },
  "message": "تم تغيير رقم الجوال بنجاح."
}
```

> 🔴 **مهم للتطبيق:** بعد النجاح كل التوكنات القديمة بتتلغي (لأنها مربوطة
> بالهوية القديمة). **لازم تستبدلوا التوكن المخزّن بالتوكن الجديد اللي في الرد**،
> وإلا كل الطلبات بعدها هترجع 401.

الرقم الجديد بيتخزن بنفس تركيب `/login` بالحرف (`country_code . phone`)، فتسجيل
الدخول بالرقم الجديد اتّجرّب وشغّال.

كمان في `/show-profile` و `/update-profile` فيه حقل جديد `pending_phone_change`
(بيبقى `null` أو فيه الرقم المستنّي + `expires_at`) لو حبيتوا تعرضوا حالة "في
انتظار التأكيد".

**دلوقتي تقدروا تشيلوا رسالة "للتغيير كلّم الدعم" وتفتحوا الخانة تاني.**

---

## 3) `grade` / `semester` بنفس النوع

في **كل** endpoint بيرجّع `user` (`/show-profile`, `/update-profile`, `/login`,
`/activateAccount`, `/profile/phone/confirm-change`) الحقلين بيرجعوا `int` أو
`null` — مفيش string خلاص.

السبب اللي كان: الموديل بعد الحفظ كان لسه شايل القيمة الخام (string) اللي جاية
من الـ form-data. اتصلح بـ 3 حاجات: cast على الموديل + `refresh()` بعد الحفظ +
cast صريح في الـ resource.

اتضاف كمان `grade_id` و `semester_id` بنفس القيمة (أسماء أوضح) — القديمين
`grade` / `semester` فاضلين زي ما هم للتوافق.

---

## 4) `search` على `POST /subjects`

```
POST /subjects
grade_id    = 12
semester_id = 2
search      = "رياض"       # اختياري
```

البحث بيتنفّذ **على مستوى الداتابيز** (مش في الذاكرة) فهيفضل شغّال بعد أي
pagination. بيدوّر على:

- `name_ar` / `name_en` للمادة
- اسم المادة الأساسية (core subject)
- **اسم المدرّس**

الـ `%` و `_` بيتعاملوا كنص عادي مش wildcards. نفس الباراميتر بيشتغل كمان على
`POST /subjects/purchased`.

---

## 5) `rating` / `tag` / `category`

كل مادة في `/subjects` و `/subjects/purchased` بقى فيها:

```json
{
  "rating": 4.5,
  "tag": "most_requested",
  "tag_label": "الأكثر طلبًا",
  "category": { "id": 1, "name": "رياضيات" }
}
```

- `rating` → `float` من 0 لـ 5، أو `null` لو مش مدخّل
- `tag` → واحد من `recommended` / `most_requested` / `new` أو `null`
- `tag_label` → نص الشارة جاهز بلغة الطلب (استخدموه على الـ badge مباشرة)
- `category` → المادة الأساسية (core subject) اللي بتجمع نسخ المادة على كل
  الصفوف والفصول، أو `null`

> ⚠️ الحقول موجودة في الـ API بس **القيم لسه فاضية** — محتاجة حد يدخّل التقييم
> والشارة لكل مادة من لوحة التحكم (الخانتين اتضافوا في شاشة إضافة/تعديل المادة).
> يعني تقدروا ترجّعوا الفلتر والترتيب، بس هيبان بعد إدخال البيانات.

---

## 6) ملفات PDF

### مسار جديد للتنزيل

كل `note` و `exam` بقى فيه حقل جديد **`download_url`** جنب `file` القديم:

```json
{
  "id": 3,
  "title": "مذكرة الوحدة الأولى",
  "type": "note",
  "file": "https://shottarapp.com/storage/files/xxx.pdf",
  "download_url": "https://shottarapp.com/api/material-file/note/3",
  "is_free": true
}
```

**استخدموا `download_url`** — بيضمن:

- ✅ `Content-Type: application/pdf`
- ✅ `Content-Length` صح (أي تنزيل ناقص يبان)
- ✅ `404` **JSON** لو الملف مش موجود — **مش 200 مع صفحة HTML**
- ✅ `403` JSON لو الملف مدفوع والمستخدم مش مشترك
- ✅ `401` JSON لو التوكن ملغي
- ✅ مفيش redirect لاستضافة تانية — الرد النهائي هو الملف نفسه
- ✅ `Accept-Ranges: bytes` + اسم ملف متكوّد صح (RFC 5987)

المسار محتاج `Authorization: Bearer <token>` زي أي endpoint تاني.
`file` القديم فاضل شغّال زي ما هو، فمفيش حاجة بتتكسر.

### الرد على سؤالكم عن `type`

**`notes[].id` و `exams[].id` تسلسلين مستقلين تمامًا** — جدولين مختلفين
(`course_materials` و `exams`). فعلًا ممكن يكون فيه اختحان رقم 5 ومذكرة رقم 5.

`type` موجود ومختلف على الاتنين: `"note"` للمذكرات و `"exam"` للاختبارات، وهو
جزء من `download_url` كمان. **الكاش لازم يكون على `type` + `id` مع بعض** — الحل
اللي عملتوه صح.

---

## 7) JSON ولا form-data

**الاتنين مقبولين** — اتّجرّبوا الاتنين على `/update-profile`:

- `application/json` ✅
- `multipart/form-data` ✅ (لازم لما فيه صورة)

المنطق اللي عندكم حاليًا (multipart لما فيه صورة، JSON لما مفيش) صح وسيبوه.

---

## 8) 401 على التوكن الملغي

### 🔴 الافتراض اللي بنيتوا عليه كان غلط — وده كان السبب الحقيقي

**كل** أخطاء الـ API كانت بترجع **HTTP 400**، مهما كان الخطأ. توكن ملغي كان
بيرجع `400`، endpoint مش موجود `400`، method غلط `400`. عشان كده التطبيق
مكانش يقدر يفرّق بين "الجلسة خلصت" و"فيه غلط في البيانات".

اتصلح، ودلوقتي:

| الحالة | الكود | الشكل |
|---|---|---|
| توكن ناقص / ملغي / غلط | **`401`** | JSON |
| خطأ صلاحية | `403` | JSON |
| مورد / endpoint مش موجود | `404` | JSON |
| method غلط | `405` | JSON |
| خطأ في البيانات (validation) | `400` *(زي ما كان)* | JSON + `data` فيه تفاصيل الحقول |
| طلبات كتير | `429` | JSON |
| خطأ سيرفر | `500` | JSON |

كل الردود JSON بالشكل المعتاد `{ "status": false, "message": "..." }` — مفيش
صفحات HTML خلاص. اتّجرّبت كلها.

> ملاحظة: كود الـ validation سيبناه **400** زي ما كان (مش 422) عشان مفيش حاجة
> عندكم تتكسر، بس الرسالة بقت رسالة الحقل الحقيقية بدل
> "The given data was invalid."، وفيه `data` فيه أخطاء كل حقل.

كمان كان فيه إشعار PHP deprecation بيتطبع **جوه جسم الرد** قبل الـ JSON على
بعض المسارات (كان بيبوّظ الـ parsing) — اتصلح.

### الرد على سؤالكم عن مدة الصلاحية

**التوكن مالوش مدة انتهاء خالص** (`sanctum.expiration = null`). يعني:

- مفيش refresh token ومفيش حاجة محتاجة تجديد صامت
- التوكن بيبقى صالح لحد ما يتلغي: تسجيل خروج، حذف الحساب، أو **تغيير رقم الجوال**
- فالـ 401 معناه دايمًا "التوكن اتلغي" مش "خلصت مدته" → روّحوا على اللوجين مباشرة

---

## 9) `terms` / `privacy_policy`

### السبب الحقيقي (اتصلح)

الخانة في لوحة التحكم كانت `<input type="text">` — **سطر واحد**. المتصفح بيبعت
النص كله في سطر واحد وكل الـ `\n` بتضيع عند الحفظ. مش مشكلة تنظيف نص، الخانة
نفسها كانت غلط.

اتعمل:

1. الخانة بقت `textarea` كبيرة (20 سطر) بتحافظ على الأسطر بالظبط + بتعرض عدد الحروف.
2. عمود `settings.value` بقى `LONGTEXT` بدل `TEXT`. **دي كانت مشكلة صامتة تانية**:
   `TEXT` سعته 65,535 **بايت**، والحرف العربي بياخد بايتين — يعني نص 33,500 حرف
   عربي بيتجاوز الحد وبيتقطّع من غير أي رسالة خطأ.
3. الـ API بيرجّع النص زي ما هو بأسطره (توحيد CRLF لـ `\n` بس).
4. لو النسخة الإنجليزية فاضية، الـ API بيرجّع العربي بدلها بدل ما يرجّع فراغ.

### إصلاح النص القديم

فيه أمر artisan بيصلح النص الموجود:

```bash
php artisan settings:fix-legal          # تشخيص بس، مش بيكتب حاجة
php artisan settings:fix-legal --fix    # التنفيذ الفعلي
```

بيعمل، من غير ما يغيّر أي كلمة:

- يشيل الـ `]` الزايدة (§9.3)
- يحوّل `\t•\t` لسطر جديد بنقطة
- يحوّل العناوين المرقّمة (`1)` … `10)`) لسطور بفاصل فقرة
- يحوّل المسافات المزدوجة لأسطر
- وبيحذّر لو `terms` == `privacy_policy` أو لو النسخة الإنجليزية فاضية/نفس العربي

الأمر **idempotent** — لو النص أصلًو فيه أسطر مبيلمسهوش.

### 🟡 الحاجتين المتبقيين محتاجين إدخال بيانات (مش كود)

- **`terms` و `privacy_policy` نفس النص بالحرف** — حد لازم يدخّل نص الشروط الصح
  من لوحة التحكم. الكود مش بيقدر يخترع محتوى.
- **النسخة الإنجليزية** — لازم تتدخّل ترجمة فعلية، وإلا الـ API هيرجّع العربي.

**الاتفاق على الصيغة:** نص عادي بـ `\n` (مش HTML). سيبوا التنسيق اللي عملتوه
في التطبيق يشتغل على الأسطر الحقيقية.

---

## 10) لينكات السوشيال

اللينكات الغلط في الداتابيز لسه محتاجة تتصحح من لوحة التحكم، بس الباك بقى
**بيحمي التطبيق منها**: أي لينك مش لينك حساب حقيقي على المنصة بيرجع **`null`**
فالأيقونة تختفي لوحدها بدل ما تفتح لينك غلط.

| الحقل | القيمة الحالية في الإنتاج | اللي الـ API بيرجّعه دلوقتي |
|---|---|---|
| `instagram` | `https://www.instagram.com/shottarkw?…` | نفسه ✅ |
| `twitter` | `https://wa.me/96550245099` | **`null`** (دومين مش تويتر) |
| `tiktok` | `https://www.tiktok.com/` | **`null`** (مفيش حساب في المسار) |
| `phone` | `+96550245099` | نفسه ✅ |

### حقل جديد: `whatsapp`

```json
{ "phone": "+96550245099", "whatsapp": "https://wa.me/96550245099" }
```

مبني على `phone` مباشرة. **استخدموه لكارت الواتساب**، وبسّطوا الشرط: الكارت
يظهر على `whatsapp` بس — مش على `phone` **أو** `twitter`.

كمان اللوحة بقت ترفض حفظ لينك من دومين غلط في خانة سوشيال (يعني مش هيحصل تاني
إن لينك واتساب يتحفظ في خانة تويتر).

> باقي: حد يدخّل لينك حساب تويتر/X الحقيقي ولينك حساب تيك توك
> (`https://www.tiktok.com/@username`) — أو يسيبهم فاضيين والأيقونات تختفي.

---

## 11) `POST /order` — مين بيحسب الخصم

### الإجابة: **الباك إند** — وده اتنفّذ

كان فيه bug أسوأ من اللي وصفتوه: كود تطبيق الكوبون في `POST /order` كان
**معمول comment أصلًا**، والمتغير `$discount` مكانش معرّف. يعني `orders.discount`
كان بيتخزن **صفر دايمًا** وأي كوبون مكانش بيتحسب على السيرفر خالص. اللي كان
شغّال هو خصم التطبيق بس، الجاي جوه `total`.

دلوقتي السيرفر هو مصدر الحقيقة:

```
POST /order
payment_method_id = 3
is_all_materials  = 0
items             = 6,7
coupon_code       = TEST10      # اختياري
total             = 45          # مقبول للتوافق لكن السيرفر بيتجاهله
```

الرد فيه التفصيل كامل:

```json
{
  "status": true,
  "data": {
    "order_id": 128,
    "payment_url": "https://…",
    "payment_status": "pending",
    "subtotal": 50,
    "bundle_discount": 5,
    "coupon_discount": 4.5,
    "discount": 9.5,
    "total": 40.5,
    "coupon_code": "TEST10",
    "currency": "KWD"
  }
}
```

### 🔴 المطلوب من التطبيق

**اعرضوا `data.total` الراجع من السيرفر، وبطّلوا تحسبوا المبلغ على الجهاز.**
الفلوس والنص ميتحسبوش على الجهاز — ده اللي بيمنع الخصم المضاعف.

ترتيب الحساب على السيرفر (للمراجعة):

1. `subtotal` = مجموع أسعار المواد **من الداتابيز** (مش من الطلب)
2. خصم الباقة:
   - `is_all_materials = 1` → السعر بيبقى `grades.all_materials_price` (لو أقل من المجموع)
   - عدد مواد N → نسبة `grades.discount_N_materials`
3. خصم الكوبون على المتبقّي (مع احترام `max_discount`)
4. تقريب لـ **3 خانات** (الدينار الكويتي)

### حاجات تانية اتصلحت في نفس المسار

- **كوبون مش صالح بقى يرفض الطلب بصراحة** (منتهي / مش مفعّل / مستنفد / مش موجود)
  بدل ما الطالب يتحاسب كامل من غير ما يعرف إن الكوبون مش شغّال.
- `orders.total` كان `decimal(10,2)` — **خانتين بس** والدينار الكويتي 3 خانات.
  بقى `decimal(12,3)` (وكمان `order_items.price` و `subjects.price`).
- الطلب وبنوده بقوا في transaction واحدة (قبل كده الطلب كان بيتعمل قبل بنوده).
- عدّاد استخدام الكوبون بيزيد وقت **الدفع الفعلي** مش وقت إنشاء الطلب، فمحاولة
  دفع فاشلة مبتحرقش الكوبون. وكولباك الدفع بقى idempotent.
- `POST /coupon/check` بقى يقبل `items` (و `is_all_materials`) اختياريين
  ويرجّع نفس حساب السيرفر (`subtotal` / `discount` / `total`) — فالرقم اللي
  بتعرضوه وقت إدخال الكوبون يبقى هو نفس الرقم اللي الطلب هيتحاسب بيه.
  شكل `data` القديم (كائن الكوبون) زي ما هو + الحقول الزيادة.

### وضع الرفض الصارم

`ORDER_STRICT_TOTAL` (افتراضيًا **off**):

- **off** → لو `total` بتاعكم مختلف عن حساب السيرفر، الطلب بيكمل بحساب
  **السيرفر** والفرق بيتسجّل في اللوج تحت `Shottar order total mismatch`.
- **on** → الطلب بيترفض بـ `422` والرد فيه التفصيل الصح.

خلّيناها off عشان مفيش حاجة تتكسر عندكم النهاردة. **بلّغونا لما تنزلوا نسخة
بتعتمد على `data.total` وهنفعّلها** — وقتها أي اختلاف بيترفض بدل ما يمشي بصمت.

### عن "بيجيب فشل على طول"

مش قادرين نجاوب من غير اللوج. المسار بقى بيسجّل سبب الفشل الحقيقي من بوابة
الدفع (`Shottar order invoice failed` مع رسالة البوابة والـ order_id وطريقة
الدفع)، فأول مرة تحصل تاني هنعرف السبب بالظبط. **ابعتولنا التاريخ والوقت
والرقم اللي جرّبتوا بيه** ونشوف اللوج.

مبلغ الخصم بـ `4.950000000000001` مش هيحصل تاني — كل الحسابات مقرّبة لـ 3 خانات
على السيرفر.

---

## 12) `progress_percent`

راجع في `POST /subjects` و `POST /subjects/purchased` (وفي `GET /subjects/{id}`).
`float` من 0 لـ 100، و `0.0` لو مفيش مشاهدات — مش `null` أبدًا. فالترتيب حسب
التقدّم والشرط بتاع إظهار الزرار المفروض يشتغلوا من غير أي تغيير عندكم.

اتصلح كمان bug كان بيضرب استثناء لو المادة اتجابت من غير eager loading
(العلاقة `lessons()` مش موجودة أصلًا على الموديل)، وتوحّد فلتر `status = 1` مع
`total_lessons` عشان النسبة تبقى متسقة.

---

## اللي محتاج شغل من التطبيق

1. 🔴 **`/profile/phone/confirm-change`** — استبدلوا التوكن المخزّن بالتوكن الجديد.
2. 🔴 **`POST /order`** — اعرضوا `data.total` من السيرفر وبطّلوا تحسبوا محليًا.
3. 🟡 **PDF** — اتحوّلوا لـ `download_url`.
4. 🟡 **الواتساب** — استخدموا `whatsapp` وبسّطوا الشرط.
5. 🟡 شيلوا رسالة "للتغيير كلّم الدعم" من خانة الرقم.
6. 🟢 حدّثوا الـ Postman collection (عندكم في ريبو الفلاتر).

## اللي محتاج إدخال بيانات من لوحة التحكم

1. 🔴 نص الشروط الصح (حاليًا نفس نص سياسة الخصوصية) + ترجمة إنجليزية.
2. 🟡 لينك تويتر/X الحقيقي ولينك حساب تيك توك.
3. 🟡 `rating` و `tag` لكل مادة.

## بعد النشر

```bash
php artisan migrate --force
php artisan settings:fix-legal            # شوف التشخيص الأول
php artisan settings:fix-legal --fix      # بعدها نفّذ
php artisan config:clear
```
